/* c8 ignore start */
/**
 * 環境変数ファイル初期化結果の型定義（型定義のみのためカバレッジ対象外）
 */

export type InitResult = {
  created: Array<{ app: string; file: string }>;
  skipped: Array<{ app: string; file: string }>;
};
/* c8 ignore stop */

// EOF
