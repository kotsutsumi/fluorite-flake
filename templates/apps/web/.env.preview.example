# -----------------------------------------------------------------------------
# ウェブ · Vercel プレビュー
# -----------------------------------------------------------------------------
# 注意: このファイルはローカル開発では読み込まれません。
#       プレビュー専用の値は Vercel のプレビュー用スコープまたは
#       `.env.preview.local` で管理してください。

# 実行環境
NODE_ENV=development
NEXT_PUBLIC_ENV=preview
NEXT_PUBLIC_APP_URL=https://<preview-domain>.vercel.app

# 認証
BETTER_AUTH_URL=
BETTER_AUTH_SECRET=
AUTH_REQUIRE_ADMIN_APPROVAL=false

# API エンドポイント
NEXT_PUBLIC_API_URL=

# データベース（クライアントでは未使用）
DATABASE_PROVIDER=
DATABASE_URL=
PRISMA_DATABASE_URL=
TURSO_AUTH_TOKEN=

# -----------------------------------------------------------------------------
# EOF
