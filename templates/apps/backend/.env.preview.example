# -----------------------------------------------------------------------------
# バックエンド · Vercel プレビュー
# -----------------------------------------------------------------------------
# 注意: このファイルはローカル `next dev` では読み込まれません。
#       プレビュー専用の値は Vercel のプレビュー用スコープまたは
#       `.env.preview.local` で管理してください。

# 実行環境
NODE_ENV=development
NEXT_PUBLIC_ENV=preview
NEXT_PUBLIC_APP_URL=https://<preview-domain>.vercel.app

# 認証
BETTER_AUTH_URL=https://<preview-domain>.vercel.app
BETTER_AUTH_SECRET=
AUTH_REQUIRE_ADMIN_APPROVAL=false

# API エンドポイント
NEXT_PUBLIC_API_URL=https://<preview-domain>.vercel.app

# データベース（Turso/libSQL）
DATABASE_PROVIDER=turso
DATABASE_URL=
PRISMA_DATABASE_URL=
TURSO_AUTH_TOKEN=

# ファイルストレージ（Vercel Blob - 本番環境推奨）
# Vercelダッシュボードで作成したBlobストアのトークンを設定
# Preview環境専用のBlobストアを使用することを推奨
BLOB_READ_WRITE_TOKEN=

# -----------------------------------------------------------------------------
# EOF
