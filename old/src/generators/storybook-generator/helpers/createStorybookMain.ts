import type { ProjectConfig } from '../../../commands/create/types.js';

/**
 * Storybookメイン設定関数（代替実装）
 * 現在はcreateStorybookConfigで処理されているが、将来の関心の分離のために保持
 */

/**
 * Storybookメイン設定関数（代替）
 * 現在はcreateStorybookConfigで処理されている
 * 将来の関心の分離のために保持
 * @param _config プロジェクト設定（未使用）
 */
export async function createStorybookMain(_config: ProjectConfig) {
    // この関数は既にcreateStorybookConfigで処理されている
    // 将来の関心の分離のために保持
}
